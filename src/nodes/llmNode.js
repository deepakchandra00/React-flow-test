// llmNode.js

import React from "react";

const LLMNode = ({ id, nodeData, updateNodeField }) => {
  return (
    <>
      <div>
        <span>LLM</span>
      </div>
      <div>
        <span>This is a LLM.</span>
      </div>
    </>
  );
};

export default LLMNode;
